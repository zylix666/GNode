# To run GNode, you need these dependencies. Install these with npm.

cd /path/to/GNode 

npm --prefix . install bindings 

npm --prefix . install debug 

npm --prefix . install ref 

npm --prefix . install ref-struct 

npm --prefix . install node-ffi 

make 

make run
